version: '3.9'

services:
  frontend:
    image: blockscout-frontend:local
    platform: linux/amd64
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
